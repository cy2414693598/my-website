// 文章数据源：阶段 2 先写死在数组里（阶段 5 换成调后端接口，阶段 4 换成数据库）。
// 这个文件就是将来 article 表的"前身"——字段和数据库表会一一对应
export const articles = [
  {
    id: 'stm32-usart-receive',
    title: 'STM32 串口接收的三种姿势：轮询、中断、DMA + 空闲中断',
    date: '2026-08-24',
    tags: ['STM32', '嵌入式', 'C'],
    summary:
      '从死等标志位到 DMA 配空闲中断，串口接收的演进史，也是嵌入式思维方式的演进史。附三种方案的代码与选型建议。',
    content: `串口接收是嵌入式入门第一课，但把不定长数据帧接稳接全，能区分"学过"和"会做"。本文记录三种方式的思路与取舍。

## 一、轮询：最简单，也最浪费

主循环里死等接收寄存器非空，等到就取走：

\`\`\`c
// 最原始的轮询接收：CPU 全程被这一件事占死
while (!(USART1->SR & USART_SR_RXNE))
    ;                       // 死等，标志位不置位就一直卡在这
uint8_t ch = USART1->DR;    // 读 DR 同时自动清 RXNE
\`\`\`

优点是直白，适合调试和教学；缺点致命——等一个字节的间隙里，CPU 什么都干不了，主频稍高点的系统根本不能这么玩。

## 二、中断：事件驱动，但有不定长帧的坑

配置 RXNE 中断，每个字节到来时进一次中断服务函数：

\`\`\`c
void USART1_IRQHandler(void)
{
    if (USART1->SR & USART_SR_RXNE) {
        uint8_t ch = USART1->DR;
        rx_buffer[rx_len++] = ch;   // 收进环形缓冲区
    }
}
\`\`\`

CPU 不用死等了，但新问题出现：**帧边界怎么判断？** 波特率高、数据密时，两次中断间隔可能小于 1 µs，处理稍慢就丢字节。这时需要第三种方式。

## 三、DMA + 空闲中断：不定长帧的标准解法

DMA 在后台搬运数据完全不占 CPU；串口的 IDLE（线路空闲）标志在总线静默一个字节时间后置位，正好当作"一帧结束"的信号：

\`\`\`c
// HAL 库一行挂起接收，剩下的交给 DMA
HAL_UARTEx_ReceiveToIdle_DMA(&huart1, rx_buf, sizeof(rx_buf));

// 空闲事件回调：Size 就是本帧实际长度
void HAL_UARTEx_RxEventCallback(UART_HandleTypeDef *huart, uint16_t Size)
{
    process_frame(rx_buf, Size);                      // 拿到完整一帧
    HAL_UARTEx_ReceiveToIdle_DMA(&huart1, rx_buf, sizeof(rx_buf)); // 重新挂起
}
\`\`\`

## 选型速查

| 方式 | CPU 占用 | 难度 | 适用 |
|---|---|---|---|
| 轮询 | 100% | 低 | 教学、极简场景 |
| 逐字节中断 | 中 | 中 | 低波特率、定长协议 |
| DMA + 空闲中断 | 接近 0 | 中高 | 高波特率、不定长帧 |

有意思的是，这三种方式的演进逻辑（死等 → 事件驱动 → 硬件托管）在 Web 开发里也有镜像：轮询接口 → WebSocket 推送 → CDN 边缘分发。技术栈不同，思维模型相通。`,
  },
  {
    id: 'first-website-in-3-days',
    title: '三天上线第一个网站：一个嵌入式工程师的 Web 入门实录',
    date: '2026-08-24',
    tags: ['全栈', '建站', 'Git'],
    summary:
      '从"Node.js 是什么"到个人主页跑在 https 上，只花了三天。记录环境、Git、部署三关，以及那些让嵌入式工程师会心一笑的瞬间。',
    content: `作为一个天天和 J-Link、逻辑分析仪打交道的人，我一直觉得"建网站"是另一个世界的事。这三天走完阶段 0 和阶段 1，发现两者的底层逻辑相似得出奇。

## 第一关：环境（半天）

装 Node.js 和 Git，配 VS Code。嵌入式工程师对"工具链"三个字的理解，在这里全是加分项——Node 之于 Web，就是 Keil 之于 STM32。

## 第二关：Git 和 GitHub（半天）

\`\`\`bash
git init -b main
git remote add origin https://github.com/cy2414693598/my-website.git
git push -u origin main
\`\`\`

第一行代码没写，先学版本管理——这个顺序和很多人想象的相反，但事后证明无比正确。中间还踩了国内访问 GitHub 的坑，给 git 配上本地代理后一切丝滑。

## 第三关：手写页面 + 部署（两天）

纯手写 HTML/CSS/JS 三件套，不碰任何框架。最大的"顿悟时刻"是 CSS 变量：

\`\`\`css
:root {
  --bg: #fafafa;
  --accent: #0969da;
}
[data-theme="dark"] {
  --bg: #0a192f;
  --accent: #64ffda;
}
\`\`\`

这不就是 **#define 宏定义 + 条件编译**吗！一套样式逻辑，编译期（渲染期）切换配置。深色模式 20 行代码搞定。

## 部署：比烧录还快

推送代码到 GitHub，打开 Pages 开关，一分钟后 https 链接生效。没有杜邦线，没有驱动装不上，没有"检测不到目标芯片"。虽然领域不同，但"程序跑在真实硬件/真实网络上"的踏实感是一样的。

## 三天小结

- 硬件背景的迁移优势：工具链思维、看手册（文档）的习惯、对"分层"的天然理解
- 最大观念转变：从"写进 flash 就完事"到"交付物是持续演进的活服务"
- 下一步：用 Vue 重构，然后是 Spring Boot 后端——继续用学 STM32 的方式学 Web：小步快跑，每步都跑通`,
  },
  {
    id: 'why-embedded-learn-web',
    title: '嵌入式工程师为什么要学 Web 全栈：技术栈拓展笔记',
    date: '2026-08-24',
    tags: ['全栈', '思考'],
    summary:
      '不是转型，是拓展。嵌入式 + Web 的交叉地带正在变成高价值区：设备看板、OTA 管理、数据可视化。说说我的动机、路线和预期。',
    content: `先把立场说清楚：我本职是嵌入式工程师，Web 是我给自己拓展的第二技术栈，不是转行跑道。这个定位决定了学习方式——不求替代，只求连接。

## 为什么是 Web 全栈

**1. 嵌入式 + Web 的交叉地带正在升值。** 现在的智能设备，固件之外几乎必然配一个云端：设备管理看板、OTA 升级页面、传感器数据可视化、告警推送。这些"设备配套的 Web 面"既懂硬件又懂 Web 的人来做，沟通成本最低，做出的东西最贴合。

**2. 全栈链路是一次完整的工程训练。** 数据库、鉴权、部署、HTTPS、域名、运维——这些是任何技术方向都通用的工程素养。就像学过 RTOS 的人再学裸机状态机是降维打击，学过完整 Web 链路的人看任何系统架构都更通透。

**3. 输出需要一个家。** 技术笔记散在各个 App 里是碎片，放在自己从零搭建的网站上，笔记本身和承载它的网站，共同构成能力的证明。

## 我的路线（进行中）

\`\`\`text
阶段 1  手写 HTML/CSS/JS + GitHub Pages        ✅ 已完成
阶段 2  Vue 3 组件化 + 文章路由 + 代码高亮     🚧 进行中
阶段 3  Spring Boot 接口（Java 后端）
阶段 4  MySQL + MyBatis-Plus
阶段 5  前后端联通，文章从数据库来
阶段 6  登录 + 管理后台
阶段 7  云服务器 + Nginx + HTTPS
\`\`\`

选 Java 而不是 Node，是因为已有基础可以复用——学新技术最贵的不是技术本身，是切换成本。

## 预期管理

不指望三个月变成"前端工程师"，目标是：**半年后能独立做出设备的 Web 管理面，一年后能独立交付一套"固件 + 云端"的完整方案。** 拓展技术栈的意义不是变成另一个人，而是让原来的自己变宽。`,
  },
]
